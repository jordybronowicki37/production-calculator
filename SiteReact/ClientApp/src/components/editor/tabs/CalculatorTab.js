import {Calculator} from "../../calculator/Calculator";

export function CalculatorTab({worksheet, products, recipes}) {
  return <Calculator worksheet={worksheet} products={products} recipes={recipes}/>
}
